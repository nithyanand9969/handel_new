import { ClientRegisteration } from './client-registeration';

describe('ClientRegisteration', () => {
  it('should create an instance', () => {
    expect(new ClientRegisteration()).toBeTruthy();
  });
});
